sulu_comment.comments:
    type: rest
    name_prefix: sulu_comment.
    resource: sulu_comment.comment_controller
    options:
        expose: true

sulu_comment.threads:
    type: rest
    name_prefix: sulu_comment.
    resource: sulu_comment.thread_controller
    options:
        expose: true
